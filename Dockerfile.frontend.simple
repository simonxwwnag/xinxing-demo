# 前端Dockerfile - 简化版（使用预构建的文件）
# 使用方法：先在本地构建前端，然后使用此Dockerfile

FROM nginx:alpine

# 复制构建好的前端文件
COPY frontend/dist /usr/share/nginx/html

# 复制nginx配置
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]

